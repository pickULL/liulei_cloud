spring:
  application:
    name: liulei-spring-cloud-config
  cloud:
    config:
      server:
        git:
          uri: https://github.com/pickULL/liulei-cloud-config.git
          username: pickULL
          password: liu19930121
          basedir: F:\ideaProjects\cloudConfig
          search-paths: /**
eureka:
  client:
    service-url:
      defaultZone: http://user:password123@localhost:8761/eureka
server:
  port: 8974
